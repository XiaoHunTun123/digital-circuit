# Day 68｜脉冲、555 与转换器强化｜第 3 轮

- 日期：2026-11-09
- Day 编号：68
- 阶段：强化｜专题训练
- 内容版本：逐日教师讲义修订版；本文件内包含题设、解法与答案。
- 学习状态：未开始；文档更新不代表个人已学完。

## 老师的话

今天的学习主线是：脉冲整形与施密特 → 555 原理与应用 → D/A 转换 → A/D 转换。先读定义与方法，随后逐题尝试，再展开解析。做题遇到困难时先只看方法标签，回到题面列出已知条件；对照答案后再遮住答案做一次，才能判断是否真正会用。

本课为综合训练：下面的知识小节覆盖整套题所需的工具。先根据目录选择薄弱部分复习，再做混合题；每道题的解析会明确对应的方法。

## 一、当天学习目标

1. 能解释“稳态数量”，写出它成立的条件，并在题目中指出使用位置。
2. 能解释“RC 变化”，写出它成立的条件，并在题目中指出使用位置。
3. 能解释“内部阈值”，写出它成立的条件，并在题目中指出使用位置。
4. 能解释“单稳态”，写出它成立的条件，并在题目中指出使用位置。
5. 能解释“理想码值模型”，写出它成立的条件，并在题目中指出使用位置。
6. 完整独立做出本课例题，能够说明每次变形或状态更新的依据。
7. 闭卷完成本文件的 12 道作业并按答案订正；核心题不允许只写结论。

## 二、建议用时与知识点顺序

建议有效学习共 **200 分钟**。每 50–60 分钟休息 10 分钟，休息不计入有效时间；时长是安排参考，验收以独立解题为准。

| 学习段 | 分钟 | 要做的事 |
|---|---:|---|
| 入门自测 | 10 | 独立做三道起点题 |
| 知识讲解 | 55 | 按概念、表格、实现顺序学习 |
| 例题精练 | 50 | 8 道例题先尝试，再逐行验证 |
| 当天作业 | 60 | 闭卷完成 12 题，表格不跳行 |
| 复盘验收 | 25 | 对答案、写错因与次日补救 |

### 入门自测（先不看答案）

1. 电容从 0 经 RC 向 5V 充电，RC=6ms，达到 2.5V 需多久？
2. 标准 555 供电 21V，控制端采用内部默认分压，给两个比较阈值。
3. 6 位理想单极性 DAC，Vref=8V，D=2，采用 Vout=Vref·D/2ⁿ，求输出与 LSB。

<details>
<summary>自测答案与补救起点</summary>

1. 6ln2 ms≈4.1589ms。 方法：一阶指数解阈值。
2. 7V、14V（仅按理想模型算，实际供电须满足器件额定）。 方法：三等分电压。
3. LSB=0.125V，Vout=0.25V。 方法：码值乘步距。

</details>

答对不足两题时，从相应定义重读，再进入例题；自测不计入正式作业得分。

## 三、具体知识讲义与核心公式

### 1. 脉冲整形与施密特

#### 稳态数量

单稳态有一个稳态，触发进入暂稳态后自动返回；多谐振荡无稳态、持续翻转；双稳态有两个稳定状态。

#### RC 变化

一阶电容电压 v(t)=V∞+(V0−V∞)exp(−t/RC)。用终值、初值和阈值解时间，比只背常数更可靠。

#### 施密特滞回

上升达到高阈值切换，下降到低阈值才切回；二者差是滞回宽度。位于两阈值之间时输出依历史保持，不能仅凭当前输入判。

#### 实际电路

输出是否反相按器件规定，频率和脉宽公式依具体拓扑。先画电容电压，再根据比较器和反相关系画输出。

#### 从定义到完整推导

从电容充放电方程推时间。已知初值 V0、最终值 V∞，一阶电路为 v(t)=V∞+(V0−V∞)e^(−t/RC)。

例：从 0V 向 9V 充电，阈值为 6V，RC=2ms。代入 6=9(1−e^(−t/2ms))，得到 e^(−t/2ms)=1/3，所以 t=2ln3 ms≈2.197ms。

若改为从 3V 开始，6=9+(3−9)e^(−t/2ms)，得到 e^(−t/2ms)=1/2，t=2ln2 ms≈1.386ms。

同一 R、C、终值、阈值，只因初值不同就改变时间。这说明直接背“约 1.1RC”必须配套使用它的充电初值和阈值条件。

本专题易错检查：把中间滞回区输出强行指定；单稳态与振荡器混用。

### 2. 555 原理与应用

#### 内部阈值

标准 555 基本模型由分压器产生 1/3 VCC 和 2/3 VCC。触发电压低于下阈值使置位，阈值输入高于上阈值使复位，放电管配合电容回落。

#### 单稳态

标准单稳态从零向 VCC 经 R 充电，到 2/3 VCC 结束，t=RC ln3≈1.099RC。推导用 1−exp(−t/RC)=2/3。

#### 无二极管多谐振荡

标准 RA、RB 电路充电经过 RA+RB，放电经过 RB。tH=ln2(RA+RB)C，tL=ln2 RB C；T=ln2(RA+2RB)C。

#### 占空比限制

标准连接 D=(RA+RB)/(RA+2RB)，RA>0 时大于 1/2。若要求小于一半需要改变充放电路径，不能套本式硬解负电阻。

#### 从定义到完整推导

推导标准无二极管 555 多谐的两段时间。电容在 VCC/3 与 2VCC/3 间变化。

充电通过 RA+RB 向 VCC：2VCC/3=VCC+(VCC/3−VCC)e^(−tH/((RA+RB)C))，整理得指数为 1/2，所以 tH=ln2(RA+RB)C。

放电通过 RB 向 0：VCC/3=(2VCC/3)e^(−tL/(RBC))，同样得 tL=ln2 RBC。

取 RA=10kΩ、RB=20kΩ、C=1μF，tH≈20.794ms，tL≈13.863ms，周期≈34.657ms，频率≈28.854Hz，占空比 3/5。这个频率不是 1/tH，必须先把两段相加。

本专题易错检查：单稳态 ln3 与振荡 ln2 混用；忘标标准拓扑。

### 3. D/A 转换

#### 理想码值模型

本课采用无符号单极性理想模型 Vout=Vref·D/2ⁿ，D 从 0 到 2ⁿ−1。最大码输出小于 Vref 一个 LSB；有些题定义满刻度不同，必须先声明模型。

#### 分辨率

一个 LSB 为 Vref/2ⁿ；位数每加 1，步距减半。分辨率不等于绝对精度，电阻误差、偏置、非线性均会影响实际输出。

#### 权网络

二进制加权电阻直接产生位权，R−2R 梯形只用两种阻值便于匹配。反相运放结构输出可能为负，不能无视题图。

#### 动态

建立时间是码字变化后到输出进入规定误差带的时间。多位同时翻转可能出现毛刺，稳态公式不能画出瞬态细节。

#### 从定义到完整推导

四位理想 DAC，Vref=8V，采用 Vout=8D/16。步距 Δ=0.5V。

输入 1011 是 D=11，所以输出 5.5V。也可按位权相加：最高位贡献 4V，第二位为 0，第三位贡献 1V，最低位贡献 0.5V，总计 5.5V。

最大码 1111 对应 7.5V，比 8V 少 0.5V。若要求精确输出 6.2V，本模型没有对应码；最近码为 D=12、输出 6V，差 -0.2V。

如果实际电路为反相运放求和，理想输出前面可能有负号；这要从电路结构确认，不从二进制码本身决定。

本专题易错检查：最大码当参考电压；分辨率与准确度混同。

### 4. A/D 转换

#### 四个步骤

采样取时间点，保持维持电压，量化把连续幅值分到区间，编码给二进制。采样率与位数分别控制时间、幅值分辨能力。

#### 量化约定

本课取输入 [0,Vref)，n 位理想截断量化 D=floor(Vin/Δ)，Δ=Vref/2ⁿ。若使用四舍五入量化要另说，二者边界和误差范围不同。

#### 结构

闪速通常需 2ⁿ−1 比较器；逐次逼近逐位比较，理想 n 次决策；双积分对恒定参考积分，速度低但可抑制周期干扰。

#### 采样条件

带限信号理想无混叠需要采样率大于两倍最高频率，等号边界需附加条件，实际留余量并做抗混叠滤波。不能从 n 位推采样速度。

#### 从定义到完整推导

四位 ADC，输入范围 [0,8)V，截断量化 Δ=0.5V，输入 5.7V。Vin/Δ=11.4，向下取整为 D=11，编码 1011。

该代码代表区间 [5.5,6.0)，所以不能仅从代码断言输入恰为 5.5V。若以区间下端作为重构值，误差为 -0.2V；最近量化是另一种规则，不能混用。

以四位逐次逼近说明比较顺序：先试 1000→4V，可保留；再试 1100→6V，过高撤回；试 1010→5V，保留；试 1011→5.5V，保留，最终 1011。这里忽略比较和建立误差，实际转换还包含采样时间。

本专题易错检查：舍入和截断混用；把采样率只设为信号频率。

## 四、典型例题：先尝试，再展开完整解答

### 例 1｜一阶指数解阈值

电容从 0 经 RC 向 5V 充电，RC=6ms，达到 2.5V 需多久？

<details>
<summary>例 1：解题步骤、答案与检查</summary>

2.5=5(1−exp(−t/RC))，得 exp(−t/RC)=1/2，所以 t=RC ln2。

**答案：** 6ln2 ms≈4.1589ms。

**验算或边界检查：** 终值 5V，半终值在 0.693 个时间常数处。

</details>

### 例 2｜三等分电压

标准 555 供电 21V，控制端采用内部默认分压，给两个比较阈值。

<details>
<summary>例 2：解题步骤、答案与检查</summary>

低阈值 VCC/3=7V，高阈值 2VCC/3=14V。

**答案：** 7V、14V（仅按理想模型算，实际供电须满足器件额定）。

**验算或边界检查：** 控制端外接改变参考时不能仍套固定分压。

</details>

### 例 3｜码值乘步距

6 位理想单极性 DAC，Vref=8V，D=2，采用 Vout=Vref·D/2ⁿ，求输出与 LSB。

<details>
<summary>例 3：解题步骤、答案与检查</summary>

Δ=8/64V，输出=2Δ。

**答案：** LSB=0.125V，Vout=0.25V。

**验算或边界检查：** 最大码为 2ⁿ−1，参考端点不是可输出码。

</details>

### 例 4｜明确区间后向下取整

7 位理想 ADC，范围 [0,8)V，截断量化，输入 5.82812V，求代码。

<details>
<summary>例 4：解题步骤、答案与检查</summary>

Δ=8/128V，Vin/Δ=93.25，取 floor=93。

**答案：** D=93，二进制 1011101。

**验算或边界检查：** 本题采用截断，不应用四舍五入；最高代码仍为 codes−1。

</details>

### 例 5｜比较方向与历史状态

非反相施密特高阈值 3V、低阈值 2V，初输入 1V、输出低；输入依次 2.5,3.2,2.5,1.8V，求输出。

<details>
<summary>例 5：解题步骤、答案与检查</summary>

上升到 2.5 未过高阈值保持低；3.2 变高；下降到 2.5 未过低阈值保持高；1.8 变低。

**答案：** 低、高、高、低。

**验算或边界检查：** 同样 2.5V 出现两种输出，体现滞回记忆。

</details>

### 例 6｜充到 2/3 终值

标准 555 单稳态 R=50kΩ，C=2μF，电容从零充电，求脉宽。

<details>
<summary>例 6：解题步骤、答案与检查</summary>

t=RC ln3，RC=100ms。

**答案：** 100ln3 ms≈109.861ms。

**验算或边界检查：** kΩ·μF=ms；不是微秒也不是秒。

</details>

### 例 7｜最大码代入模型

6 位 DAC，Vref=4V，求最大码输出。

<details>
<summary>例 7：解题步骤、答案与检查</summary>

Dmax=63，输出=4·Dmax/64。

**答案：** 3.9375V。

**验算或边界检查：** 与 4V 相差恰一个 LSB。

</details>

### 例 8｜结构数量

7 位闪速 ADC 理想需要多少比较器？逐次逼近理想需几次逐位判定？

<details>
<summary>例 8：解题步骤、答案与检查</summary>

闪速内部阈值数 2^7−1；逐次逼近从最高位到最低位各判一次。

**答案：** 127 个比较器；SAR 7 次判定。

**验算或边界检查：** 实际 SAR 转换时钟还可能包含采样等开销。

</details>

## 五、当天作业：12 道具体题目

每题 1 分，共 12 分。结果正确且关键步骤完整得 1 分，方法正确但局部算错得 0.5 分；只有答案、不能解释依据得 0 分。计算/设计题按独立过程判分，文字题需写理由。

### A 组｜基础与方法落实

1. 电容从 0 经 RC 向 5V 充电，RC=4ms，达到 2.5V 需多久？

2. 标准 555 供电 15V，控制端采用内部默认分压，给两个比较阈值。

3. 6 位理想单极性 DAC，Vref=8V，D=26，采用 Vout=Vref·D/2ⁿ，求输出与 LSB。

4. 7 位理想 ADC，范围 [0,8)V，截断量化，输入 3.32812V，求代码。

5. 标准 555 单稳态 R=30kΩ，C=2μF，电容从零充电，求脉宽。

6. 电路触发后输出高保持一段时间再自动回低；无外触发不振荡，属于哪类？


### B 组｜变式与综合应用

7. 标准无二极管 555 多谐，RA=70kΩ，RB=20kΩ，C=1μF，求高低时间、频率和占空比。

8. 参考电压不变，DAC 从 6 位增至 8 位，LSB 怎样变化？

9. 带限信号最高 300Hz，候选采样率 300Hz、900Hz，哪个满足理想无混叠裕量？

10. 电容初压 5V 向 0 放电，RC=4ms，降至 5/e V 的时间？

11. 标准 555 无二极管多谐要求占空比 40%，能否用正 RA,RB 达到？

12. R−2R 网络的主要结构优势是什么？输出运放为反相型时符号怎样？

## 六、作业答案与关键步骤

<details>
<summary>全部完成后展开逐题答案</summary>

### 作业 1

**方法：** 一阶指数解阈值

2.5=5(1−exp(−t/RC))，得 exp(−t/RC)=1/2，所以 t=RC ln2。

**答案：** 4ln2 ms≈2.7726ms。

**核对：** 终值 5V，半终值在 0.693 个时间常数处。

### 作业 2

**方法：** 三等分电压

低阈值 VCC/3=5V，高阈值 2VCC/3=10V。

**答案：** 5V、10V（仅按理想模型算，实际供电须满足器件额定）。

**核对：** 控制端外接改变参考时不能仍套固定分压。

### 作业 3

**方法：** 码值乘步距

Δ=8/64V，输出=26Δ。

**答案：** LSB=0.125V，Vout=3.25V。

**核对：** 最大码为 2ⁿ−1，参考端点不是可输出码。

### 作业 4

**方法：** 明确区间后向下取整

Δ=8/128V，Vin/Δ=53.25，取 floor=53。

**答案：** D=53，二进制 0110101。

**核对：** 本题采用截断，不应用四舍五入；最高代码仍为 codes−1。

### 作业 5

**方法：** 充到 2/3 终值

t=RC ln3，RC=60ms。

**答案：** 60ln3 ms≈65.917ms。

**核对：** kΩ·μF=ms；不是微秒也不是秒。

### 作业 6

**方法：** 数稳定状态

低是唯一长期稳定状态，高是有限持续暂稳态，需外触发进入。

**答案：** 单稳态。

**核对：** 若不触发也持续周期翻转才是多谐振荡器。

### 作业 7

**方法：** 分清充放电路径

tH=ln2(RA+RB)C=62.383ms；tL=ln2 RB C=13.863ms；先求周期再取倒数。

**答案：** T=76.246ms，f=13.115Hz，D=0.8182。

**核对：** 无二极管且 RA 正时 D>0.5。

### 作业 8

**方法：** 位数指数关系

代码数扩大 2²=4 倍，所以步距除 4。

**答案：** LSB 变原来的 1/4。

**核对：** 分辨率提高不自动保证噪声或非线性也改善四倍。

### 作业 9

**方法：** 比较两倍最高频率

两倍上界为 600Hz，第一个低于此值，第二个高于。

**答案：** 选 900Hz。

**核对：** 实际还需抗混叠滤波与带宽条件。

### 作业 10

**方法：** 放电指数

v=5exp(−t/RC)，令 v=5/e 得 −t/RC=−1。

**答案：** 4ms。

**核对：** 一个时间常数后保留初值的约 36.8%。

### 作业 11

**方法：** 分析公式范围

D=(RA+RB)/(RA+2RB)=1/2+RA/[2(RA+2RB)]，正电阻时严格大于 1/2。

**答案：** 不能；需改变拓扑或输出使用方式。

**核对：** 解出负电阻不是有效元件选择。

### 作业 12

**方法：** 阻值种类与电路极性

只使用 R 与 2R 两种阻值，便于匹配；若按反相求和结构连接，码值幅度前有负号。

**答案：** 两种阻值；符号按反相连接为负。

**核对：** 不能把单极性正输出模型无条件套反相电路。

</details>

## 七、复盘问题：闭卷口述

1. “稳态数量”与“RC 变化”分别解决什么问题？从本课各举一题。
2. 针对“把中间滞回区输出强行指定；单稳态与振荡器混用。”，指出你今天哪道题需要检查这一步。
3. “内部阈值”与“单稳态”分别解决什么问题？从本课各举一题。
4. 针对“单稳态 ln3 与振荡 ln2 混用；忘标标准拓扑。”，指出你今天哪道题需要检查这一步。
5. “理想码值模型”与“分辨率”分别解决什么问题？从本课各举一题。
6. 针对“最大码当参考电压；分辨率与准确度混同。”，指出你今天哪道题需要检查这一步。
7. 今天哪一步必须使用题设条件？去掉它后原答案是否还成立？
8. 不翻答案，复述一题的“识别—列式/列表—求解—检查”四步。
9. 例题会做而变式不会做时，究竟是数据改变、条件改变还是目标改变？

## 八、当天验收标准

- [ ] 能闭卷写出本课核心关系及适用条件。
- [ ] 例题已经独立尝试，错题已遮住解析重做。
- [ ] 12 道作业至少得到 10 分，基础组至少 5/6 分。
- [ ] 表格题逐行填写，计算题保留关键推导；不存在只抄最终答案的情况。
- [ ] 至少写出一个具体错因，以及下一次先检查什么。

时间表完成不自动代表通过；正确率和过程检查都达到才记录已完成。未达标时按下面计划补救，保持学习日期索引，另记实际完成日期。

## 九、30 分钟补救与间隔复习

1. 5 分钟：选出两道失分题，定位错误发生的第一步。
2. 10 分钟：只读对应知识小节与一道例题，口头说出条件。
3. 10 分钟：遮住答案重做这两题。
4. 5 分钟：对照检查依据，仍不会则记录具体问题。

本课复习日期：2026-11-10、2026-11-12、2026-11-16。若已到正式考试后，不执行机械补课；把最后复习压缩到实际考前的轻量回忆。

## 十、今日打卡

- 实际学习日期：
- 实际有效用时：
- 自测：__/3
- 作业：__/12
- 已独立重做的例题号：
- 错题号及首次错误步骤：
- 下一次先检查：
- 最终状态：未开始 / 进行中 / 已完成 / 未达标

## 下一课

[Day 69｜逻辑代数与卡诺图强化｜第 4 轮](day69.md)。如本课未达标，先补救再继续；实际学习状态自行记录。
