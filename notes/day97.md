# Day 97｜真题错题回炉：扩展模块

- 日期：2026-12-08
- Day 编号：97
- 阶段：真题/模拟
- 内容版本：逐日教师讲义修订版；本文件内包含题设、解法与答案。
- 学习状态：未开始；文档更新不代表个人已学完。

## 老师的话

今天的学习主线是：半导体存储器与容量扩展 → ROM、PLA 与 PAL → 555 原理与应用 → A/D 转换。先读定义与方法，随后逐题尝试，再展开解析。做题遇到困难时先只看方法标签，回到题面列出已知条件；对照答案后再遮住答案做一次，才能判断是否真正会用。

本课为综合训练：下面的知识小节覆盖整套题所需的工具。先根据目录选择薄弱部分复习，再做混合题；每道题的解析会明确对应的方法。

**题目来源说明：** 本文件中的题目均为本次编写的原创练习或标准教学题型，附完整答案，不冒充任何年份或院校的真实试题。标题中的年份保留原学习日历的真题复习位置；手中有正版相应真题时，可把当日限时段用于该卷。本文件的原创题可独立完成，用于方法训练与补测，题型配比及得分不能替代官方试卷评价。

## 一、当天学习目标

1. 能解释“单位”，写出它成立的条件，并在题目中指出使用位置。
2. 能解释“位扩展”，写出它成立的条件，并在题目中指出使用位置。
3. 能解释“与或阵列”，写出它成立的条件，并在题目中指出使用位置。
4. 能解释“多输出共享”，写出它成立的条件，并在题目中指出使用位置。
5. 能解释“内部阈值”，写出它成立的条件，并在题目中指出使用位置。
6. 完整独立做出本课例题，能够说明每次变形或状态更新的依据。
7. 闭卷完成本文件的 12 道作业并按答案订正；核心题不允许只写结论。

## 二、建议用时与知识点顺序

建议有效学习共 **200 分钟**。每 50–60 分钟休息 10 分钟，休息不计入有效时间；时长是安排参考，验收以独立解题为准。

| 学习段 | 分钟 | 要做的事 |
|---|---:|---|
| 入门自测 | 10 | 独立做三道起点题 |
| 知识讲解 | 55 | 按概念、表格、实现顺序学习 |
| 例题精练 | 50 | 8 道例题先尝试，再逐行验证 |
| 当天作业 | 60 | 闭卷完成 12 题，表格不跳行 |
| 复盘验收 | 25 | 对答案、写错因与次日补救 |

### 入门自测（先不看答案）

1. 存储器 128×16 bit，需多少地址线、数据线和总字节？
2. 2 输入、3 输出的组合函数，用 ROM 真值表实现需多大容量？
3. 标准 555 供电 9V，控制端采用内部默认分压，给两个比较阈值。

<details>
<summary>自测答案与补救起点</summary>

1. 地址 7，数据 16，总 256 B。 方法：深度与宽度分别算。
2. 4×3 bit，总 12 bit。 方法：一组输入对应一个地址。
3. 3V、6V（仅按理想模型算，实际供电须满足器件额定）。 方法：三等分电压。

</details>

答对不足两题时，从相应定义重读，再进入例题；自测不计入正式作业得分。

## 三、具体知识讲义与核心公式

### 1. 半导体存储器与容量扩展

#### 单位

K 按 1024，容量写字数×字长。2ⁿ 个字需 n 根字地址线，数据位宽由字长给出。总位容量与总字节容量要换算。

#### 位扩展

多片同深度芯片共用地址与片选，不同芯片提供不同数据位。例如两片 1K×4 并行得到 1K×8。

#### 字扩展

低地址接片内地址，高地址译码生成互斥片选；数据总线共享但只有被选片可输出。

#### 地址映射

完整译码给唯一地址块，部分译码可能产生镜像。给定起始地址后用容量减一求结束地址，不能把字数直接当结束偏移。

#### 从定义到完整推导

用四片 1K×8 实现 4K×8，按字节寻址映射 0x000–0xFFF。

| 高地址 A11A10 | 被选片 | 地址范围 |
|---|---|---|
| 00 | 片0 | 0x000–0x3FF |
| 01 | 片1 | 0x400–0x7FF |
| 10 | 片2 | 0x800–0xBFF |
| 11 | 片3 | 0xC00–0xFFF |

低十位 A9…A0 并接全部芯片地址端，高两位经 2-4 译码器产生四路片选。读写控制按规格共用；八根数据线接共享总线，但未选片输出需高阻。

核算：每块 0x400=1024 字节，四块共 4096 字节。若片选只译 A11 不译 A10，就不能得到上述四个独立地址块。

本专题易错检查：地址线数按总比特数算；多片同时驱动总线。

### 2. ROM、PLA 与 PAL

#### 与或阵列

ROM 固定译码生成全部最小项、或阵列选择输出；PLA 的与、或阵列均可编程；经典 PAL 与阵列可编程、或阵列固定连接。

#### 多输出共享

多个函数含相同乘积项时可在 PLA 中共享，统计的是不同乘积项数量，不是各函数项数机械相加。

#### ROM 容量

n 输入 m 输出逻辑函数可用 2ⁿ×m 的 ROM 真值表实现，地址为输入，存储数据为输出。这是逻辑实现容量模型。

#### 映射步骤

先写每个输出的与或式，列唯一乘积项表，再画每个项接到哪些输出。检查输入是否提供反变量、项数量是否满足器件限制。

#### 从定义到完整推导

实现 F1=AB+反AC，F2=AB+BC。先列不同乘积项，而不是为每个输出独立重复做一份。

| 项 | 定义 | 接 F1 | 接 F2 |
|---|---|---|---|
| P1 | AB | 是 | 是 |
| P2 | 反AC | 是 | 否 |
| P3 | BC | 否 | 是 |

PLA 需要三条乘积线和两条输出或线。输入端需要提供 A、反A、B、C；若结构包含可用反变量，直接选相应线。

用 ROM 实现同样三个输入、两个输出则需要 8×2 bit 的真值存储。ROM 实现按全输入组合分配地址，PLA 按可共享乘积项组织，两者容量成本的计数对象不同。

本专题易错检查：PLA 与 PAL 可编程部分反记；多输出共享项重复计数。

### 3. 555 原理与应用

#### 内部阈值

标准 555 基本模型由分压器产生 1/3 VCC 和 2/3 VCC。触发电压低于下阈值使置位，阈值输入高于上阈值使复位，放电管配合电容回落。

#### 单稳态

标准单稳态从零向 VCC 经 R 充电，到 2/3 VCC 结束，t=RC ln3≈1.099RC。推导用 1−exp(−t/RC)=2/3。

#### 无二极管多谐振荡

标准 RA、RB 电路充电经过 RA+RB，放电经过 RB。tH=ln2(RA+RB)C，tL=ln2 RB C；T=ln2(RA+2RB)C。

#### 占空比限制

标准连接 D=(RA+RB)/(RA+2RB)，RA>0 时大于 1/2。若要求小于一半需要改变充放电路径，不能套本式硬解负电阻。

#### 从定义到完整推导

推导标准无二极管 555 多谐的两段时间。电容在 VCC/3 与 2VCC/3 间变化。

充电通过 RA+RB 向 VCC：2VCC/3=VCC+(VCC/3−VCC)e^(−tH/((RA+RB)C))，整理得指数为 1/2，所以 tH=ln2(RA+RB)C。

放电通过 RB 向 0：VCC/3=(2VCC/3)e^(−tL/(RBC))，同样得 tL=ln2 RBC。

取 RA=10kΩ、RB=20kΩ、C=1μF，tH≈20.794ms，tL≈13.863ms，周期≈34.657ms，频率≈28.854Hz，占空比 3/5。这个频率不是 1/tH，必须先把两段相加。

本专题易错检查：单稳态 ln3 与振荡 ln2 混用；忘标标准拓扑。

### 4. A/D 转换

#### 四个步骤

采样取时间点，保持维持电压，量化把连续幅值分到区间，编码给二进制。采样率与位数分别控制时间、幅值分辨能力。

#### 量化约定

本课取输入 [0,Vref)，n 位理想截断量化 D=floor(Vin/Δ)，Δ=Vref/2ⁿ。若使用四舍五入量化要另说，二者边界和误差范围不同。

#### 结构

闪速通常需 2ⁿ−1 比较器；逐次逼近逐位比较，理想 n 次决策；双积分对恒定参考积分，速度低但可抑制周期干扰。

#### 采样条件

带限信号理想无混叠需要采样率大于两倍最高频率，等号边界需附加条件，实际留余量并做抗混叠滤波。不能从 n 位推采样速度。

#### 从定义到完整推导

四位 ADC，输入范围 [0,8)V，截断量化 Δ=0.5V，输入 5.7V。Vin/Δ=11.4，向下取整为 D=11，编码 1011。

该代码代表区间 [5.5,6.0)，所以不能仅从代码断言输入恰为 5.5V。若以区间下端作为重构值，误差为 -0.2V；最近量化是另一种规则，不能混用。

以四位逐次逼近说明比较顺序：先试 1000→4V，可保留；再试 1100→6V，过高撤回；试 1010→5V，保留；试 1011→5.5V，保留，最终 1011。这里忽略比较和建立误差，实际转换还包含采样时间。

本专题易错检查：舍入和截断混用；把采样率只设为信号频率。

### 5. D/A 转换

#### 理想码值模型

本课采用无符号单极性理想模型 Vout=Vref·D/2ⁿ，D 从 0 到 2ⁿ−1。最大码输出小于 Vref 一个 LSB；有些题定义满刻度不同，必须先声明模型。

#### 分辨率

一个 LSB 为 Vref/2ⁿ；位数每加 1，步距减半。分辨率不等于绝对精度，电阻误差、偏置、非线性均会影响实际输出。

#### 权网络

二进制加权电阻直接产生位权，R−2R 梯形只用两种阻值便于匹配。反相运放结构输出可能为负，不能无视题图。

#### 动态

建立时间是码字变化后到输出进入规定误差带的时间。多位同时翻转可能出现毛刺，稳态公式不能画出瞬态细节。

#### 从定义到完整推导

四位理想 DAC，Vref=8V，采用 Vout=8D/16。步距 Δ=0.5V。

输入 1011 是 D=11，所以输出 5.5V。也可按位权相加：最高位贡献 4V，第二位为 0，第三位贡献 1V，最低位贡献 0.5V，总计 5.5V。

最大码 1111 对应 7.5V，比 8V 少 0.5V。若要求精确输出 6.2V，本模型没有对应码；最近码为 D=12、输出 6V，差 -0.2V。

如果实际电路为反相运放求和，理想输出前面可能有负号；这要从电路结构确认，不从二进制码本身决定。

本专题易错检查：最大码当参考电压；分辨率与准确度混同。

## 四、典型例题：先尝试，再展开完整解答

### 例 1｜深度与宽度分别算

存储器 128×16 bit，需多少地址线、数据线和总字节？

<details>
<summary>例 1：解题步骤、答案与检查</summary>

字数=128=2^7，所以地址 7 根；每字 16 位即 2 字节，总共 256 字节。

**答案：** 地址 7，数据 16，总 256 B。

**验算或边界检查：** 按字寻址模型；不能用总位数求地址根数。

</details>

### 例 2｜一组输入对应一个地址

2 输入、3 输出的组合函数，用 ROM 真值表实现需多大容量？

<details>
<summary>例 2：解题步骤、答案与检查</summary>

输入共有 2^2=4 组，每组存 3 个输出位。

**答案：** 4×3 bit，总 12 bit。

**验算或边界检查：** 容量单位明确是位，不是字节。

</details>

### 例 3｜三等分电压

标准 555 供电 9V，控制端采用内部默认分压，给两个比较阈值。

<details>
<summary>例 3：解题步骤、答案与检查</summary>

低阈值 VCC/3=3V，高阈值 2VCC/3=6V。

**答案：** 3V、6V（仅按理想模型算，实际供电须满足器件额定）。

**验算或边界检查：** 控制端外接改变参考时不能仍套固定分压。

</details>

### 例 4｜明确区间后向下取整

4 位理想 ADC，范围 [0,8)V，截断量化，输入 2.125V，求代码。

<details>
<summary>例 4：解题步骤、答案与检查</summary>

Δ=8/16V，Vin/Δ=4.25，取 floor=4。

**答案：** D=4，二进制 0100。

**验算或边界检查：** 本题采用截断，不应用四舍五入；最高代码仍为 codes−1。

</details>

### 例 5｜码值乘步距

5 位理想单极性 DAC，Vref=8V，D=15，采用 Vout=Vref·D/2ⁿ，求输出与 LSB。

<details>
<summary>例 5：解题步骤、答案与检查</summary>

Δ=8/32V，输出=15Δ。

**答案：** LSB=0.25V，Vout=3.75V。

**验算或边界检查：** 最大码为 2ⁿ−1，参考端点不是可输出码。

</details>

### 例 6｜只做位扩展

用 1K×4 芯片组成 1K×24，需几片，地址和片选怎样接？

<details>
<summary>例 6：解题步骤、答案与检查</summary>

字长比例 24/4=6 片；地址 10 根并联，片选同时有效，各片输出不同的四位数据。

**答案：** 6 片，无新增地址线。

**验算或边界检查：** 各芯片不可接同一数据位而同时输出。

</details>

### 例 7｜共享重复 AB

PLA 实现 F1=AB+AC，F2=AB+反BC，需要几条不同乘积项，各接哪里？

<details>
<summary>例 7：解题步骤、答案与检查</summary>

乘积项为 P1=AB，P2=AC，P3=反BC。F1 接 P1,P2；F2 接 P1,P3。

**答案：** 3 条乘积项，AB 共享。

**验算或边界检查：** 不能把各函数两项机械相加成四条。

</details>

### 例 8｜充到 2/3 终值

标准 555 单稳态 R=20kΩ，C=1μF，电容从零充电，求脉宽。

<details>
<summary>例 8：解题步骤、答案与检查</summary>

t=RC ln3，RC=20ms。

**答案：** 20ln3 ms≈21.972ms。

**验算或边界检查：** kΩ·μF=ms；不是微秒也不是秒。

</details>

## 五、当天作业：12 道具体题目

每题 1 分，共 12 分。结果正确且关键步骤完整得 1 分，方法正确但局部算错得 0.5 分；只有答案、不能解释依据得 0 分。计算/设计题按独立过程判分，文字题需写理由。

### A 组｜基础与方法落实

1. 存储器 32×16 bit，需多少地址线、数据线和总字节？

2. 6 输入、3 输出的组合函数，用 ROM 真值表实现需多大容量？

3. 标准 555 供电 21V，控制端采用内部默认分压，给两个比较阈值。

4. 4 位理想 ADC，范围 [0,8)V，截断量化，输入 6.125V，求代码。

5. 5 位理想单极性 DAC，Vref=8V，D=7，采用 Vout=Vref·D/2ⁿ，求输出与 LSB。

6. 标准 555 单稳态 R=60kΩ，C=1μF，电容从零充电，求脉宽。


### B 组｜变式与综合应用

7. 5 位闪速 ADC 理想需要多少比较器？逐次逼近理想需几次逐位判定？

8. 6 位 DAC，Vref=4V，求最大码输出。

9. 用 1K×8 组成 16K×8，求片数与片选译码规模。

10. 比较 ROM、PLA、经典 PAL 的与阵列、或阵列是否可编程。

11. 标准无二极管 555 多谐，RA=50kΩ，RB=20kΩ，C=1μF，求高低时间、频率和占空比。

12. 带限信号最高 600Hz，候选采样率 600Hz、1800Hz，哪个满足理想无混叠裕量？

## 六、作业答案与关键步骤

<details>
<summary>全部完成后展开逐题答案</summary>

### 作业 1

**方法：** 深度与宽度分别算

字数=32=2^5，所以地址 5 根；每字 16 位即 2 字节，总共 64 字节。

**答案：** 地址 5，数据 16，总 64 B。

**核对：** 按字寻址模型；不能用总位数求地址根数。

### 作业 2

**方法：** 一组输入对应一个地址

输入共有 2^6=64 组，每组存 3 个输出位。

**答案：** 64×3 bit，总 192 bit。

**核对：** 容量单位明确是位，不是字节。

### 作业 3

**方法：** 三等分电压

低阈值 VCC/3=7V，高阈值 2VCC/3=14V。

**答案：** 7V、14V（仅按理想模型算，实际供电须满足器件额定）。

**核对：** 控制端外接改变参考时不能仍套固定分压。

### 作业 4

**方法：** 明确区间后向下取整

Δ=8/16V，Vin/Δ=12.25，取 floor=12。

**答案：** D=12，二进制 1100。

**核对：** 本题采用截断，不应用四舍五入；最高代码仍为 codes−1。

### 作业 5

**方法：** 码值乘步距

Δ=8/32V，输出=7Δ。

**答案：** LSB=0.25V，Vout=1.75V。

**核对：** 最大码为 2ⁿ−1，参考端点不是可输出码。

### 作业 6

**方法：** 充到 2/3 终值

t=RC ln3，RC=60ms。

**答案：** 60ln3 ms≈65.917ms。

**核对：** kΩ·μF=ms；不是微秒也不是秒。

### 作业 7

**方法：** 结构数量

闪速内部阈值数 2^5−1；逐次逼近从最高位到最低位各判一次。

**答案：** 31 个比较器；SAR 5 次判定。

**核对：** 实际 SAR 转换时钟还可能包含采样等开销。

### 作业 8

**方法：** 最大码代入模型

Dmax=63，输出=4·Dmax/64。

**答案：** 3.9375V。

**核对：** 与 4V 相差恰一个 LSB。

### 作业 9

**方法：** 字扩展

共 16 片；低 10 位片内地址，高 4 位进 4 到 16 译码器控制互斥片选。

**答案：** 16 片，地址 14 根，数据 8 根。

**核对：** 未选片必须不驱动数据总线。

### 作业 10

**方法：** 按结构定义区分

| 结构 | 与阵列 | 或阵列 |
| --- | --- | --- |
| ROM | 固定译码 | 可编程 |
| PLA | 可编程 | 可编程 |
| PAL | 可编程 | 固定 |

**答案：** 如表。

**核对：** 这里采用经典结构模型，实际 CPLD/FPGA 不等同于三种简单阵列。

### 作业 11

**方法：** 分清充放电路径

tH=ln2(RA+RB)C=48.520ms；tL=ln2 RB C=13.863ms；先求周期再取倒数。

**答案：** T=62.383ms，f=16.030Hz，D=0.7778。

**核对：** 无二极管且 RA 正时 D>0.5。

### 作业 12

**方法：** 比较两倍最高频率

两倍上界为 1200Hz，第一个低于此值，第二个高于。

**答案：** 选 1800Hz。

**核对：** 实际还需抗混叠滤波与带宽条件。

</details>

## 七、复盘问题：闭卷口述

1. “单位”与“位扩展”分别解决什么问题？从本课各举一题。
2. 针对“地址线数按总比特数算；多片同时驱动总线。”，指出你今天哪道题需要检查这一步。
3. “与或阵列”与“多输出共享”分别解决什么问题？从本课各举一题。
4. 针对“PLA 与 PAL 可编程部分反记；多输出共享项重复计数。”，指出你今天哪道题需要检查这一步。
5. “内部阈值”与“单稳态”分别解决什么问题？从本课各举一题。
6. 针对“单稳态 ln3 与振荡 ln2 混用；忘标标准拓扑。”，指出你今天哪道题需要检查这一步。
7. 今天哪一步必须使用题设条件？去掉它后原答案是否还成立？
8. 不翻答案，复述一题的“识别—列式/列表—求解—检查”四步。
9. 例题会做而变式不会做时，究竟是数据改变、条件改变还是目标改变？

## 八、当天验收标准

- [ ] 能闭卷写出本课核心关系及适用条件。
- [ ] 例题已经独立尝试，错题已遮住解析重做。
- [ ] 12 道作业至少得到 10 分，基础组至少 5/6 分。
- [ ] 表格题逐行填写，计算题保留关键推导；不存在只抄最终答案的情况。
- [ ] 至少写出一个具体错因，以及下一次先检查什么。

时间表完成不自动代表通过；正确率和过程检查都达到才记录已完成。未达标时按下面计划补救，保持学习日期索引，另记实际完成日期。

## 九、30 分钟补救与间隔复习

1. 5 分钟：选出两道失分题，定位错误发生的第一步。
2. 10 分钟：只读对应知识小节与一道例题，口头说出条件。
3. 10 分钟：遮住答案重做这两题。
4. 5 分钟：对照检查依据，仍不会则记录具体问题。

本课复习日期：2026-12-09、2026-12-11、2026-12-15。若已到正式考试后，不执行机械补课；把最后复习压缩到实际考前的轻量回忆。

## 十、今日打卡

- 实际学习日期：
- 实际有效用时：
- 自测：__/3
- 作业：__/12
- 已独立重做的例题号：
- 错题号及首次错误步骤：
- 下一次先检查：
- 最终状态：未开始 / 进行中 / 已完成 / 未达标

## 下一课

[Day 98｜全真模拟一](day98.md)。如本课未达标，先补救再继续；实际学习状态自行记录。
